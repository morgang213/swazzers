// Main application entry point
console.log('Swazzers webapp initialized');

document.addEventListener('DOMContentLoaded', () => {
    const app = document.getElementById('app');
    if (app) {
        console.log('App container found and ready');
    }
});
